<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="org.zerock.mapper.EatBoardMapper">

  <select id="getList" resultType="org.zerock.domain.EatBoardVO">
    
    <![CDATA[
    
	SELECT
		eatbno eatbno,
		title title,
		content content,
		adress adress,
		writer writer,
		regdate regdate,
		updatedate updatedate
	FROM eatBoard
	where eatbno > 0 order by eatbno DESC
     
    ]]>
    
    </select>
    
    <insert id="insert">
  	INSERT INTO eatBoard (title, content, adress, writer) 
  	VALUES (#{title}, #{content}, #{adress}, #{writer})
  </insert>
  
  <insert id="insertSelectKey" useGeneratedKeys="true" keyProperty="eatbno" keyColumn="eatbno">
  	INSERT INTO eatBoard (title, content, adress, writer)
  	VALUES (#{title}, #{content}, #{adress}, #{writer})
  </insert>
  
   <select id="read" resultType="org.zerock.domain.EatBoardVO">
	SELECT * FROM eatBoard WHERE eatbno = #{eatbno}
  </select>
  
  <delete id="delete">
  	DELETE FROM eatBoard WHERE eatbno = #{eatbno}
  </delete>
	
  <update id="update">
  	UPDATE eatBoard
  	
  	SET
  	    title = #{title}, 
  	    content = #{content},
  	    adress = #{adress},
  	    writer = #{writer},
  	    updateDate = NOW()
  	    
  	WHERE
  	    eatbno = #{eatbno}
  
  </update>
</mapper>

















